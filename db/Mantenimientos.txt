-- CREAR TABLA DE MANTENIMIENTOS 

USE [statusAppsPro]
GO

/****** Object:  Table [dbo].[mantenimientos] ******/
SET ANSI_NULLS ON
GO

SET QUOTED_IDENTIFIER ON
GO

CREATE TABLE [dbo].[mantenimientos](
	[id] [int] IDENTITY(1,1) NOT NULL,
	[fechaestimada] [date] NOT NULL,
	[fecharealizada] [date] NULL,
	[economico] [varchar](6) NOT NULL,
	[constancia] [varbinary](max) NULL,
	[descripcion] [nvarchar](max) NULL,
PRIMARY KEY CLUSTERED 
(
	[id] ASC
)WITH (PAD_INDEX = OFF, STATISTICS_NORECOMPUTE = OFF, IGNORE_DUP_KEY = OFF, ALLOW_ROW_LOCKS = ON, ALLOW_PAGE_LOCKS = ON, OPTIMIZE_FOR_SEQUENTIAL_KEY = OFF) ON [PRIMARY]
) ON [PRIMARY] TEXTIMAGE_ON [PRIMARY]
GO

ALTER TABLE [dbo].[mantenimientos] ADD  CONSTRAINT [DF_fechaestimada]  DEFAULT ('2000-01-01') FOR [fechaestimada]
GO

ALTER TABLE [dbo].[mantenimientos] ADD  DEFAULT ('000000') FOR [economico]
GO

ALTER TABLE [dbo].[mantenimientos]  WITH NOCHECK ADD  CONSTRAINT [FK_mantenimientos_sucursales] FOREIGN KEY([economico])
REFERENCES [dbo].[sucursales] ([economico])
GO

ALTER TABLE [dbo].[mantenimientos] CHECK CONSTRAINT [FK_mantenimientos_sucursales]
GO


-- DATO ESPECIAL
Documentos de 2024 o mas antiguos son considerados historicos y no generan nuevos mantenimientos estimados.
(Opcional) Se podría dejar el primer elemento como la plantilla a utilizar para los mantenimientos

-- REINICIAR EL ID
DBCC CHECKIDENT ('mantenimientos', RESEED, 0);